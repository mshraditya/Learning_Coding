#include <iostream>
using namespace std;
int main() {
    int arr[2][3]={{1,2,3},{4,5,6}}; //this is how initialisation is doen here 
    //another way is 
    int arr2[2][3]={1,2,3,4,5,6};//this is also the way .
    // arr[i][j]= 'i' represent row ,'j' represent coloumn.

    // we can fetch using  loop as well as we can 
    // take input doing the same.
    
    // 2d-array can also be written like arr[][j] 
    // point to be noted that writting the column is mandatory

    return 0;
}